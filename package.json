{
  "name": "inverse-governance-subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio inverse-governance-subgraph",
    "deploy-dev": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ arr00/compound-governance-2",
    "create-local": "graph create --node http://localhost:8020/ arr00/compound-governance-2",
    "remove-local": "graph remove --node http://localhost:8020/ arr00/compound-governance-2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 arr00/compound-governance-2"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.25.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.25.0"
  }
}
